apiVersion: v1
kind: Service
metadata:
  name: mcp-k8s
  namespace: inferno
  labels:
    app: mcp-k8s
spec:
  type: ClusterIP
  selector:
    app: mcp-k8s
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
